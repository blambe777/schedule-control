{
  "config": {
    "step": {
      "user": {
        "title": "Schedule Control",
        "description": "Install the local schedule execution engine. Entity mappings are managed by the Schedule Control card."
      }
    },
    "abort": {
      "already_configured": "Schedule Control is already configured."
    }
  }
}
